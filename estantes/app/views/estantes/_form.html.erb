<%= form_with(model: estante, local: true) do |form| %>
  <% if estante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estante.errors.count, "error") %> prohibited this estante from being saved:</h2>

      <ul>
      <% estante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :estante_nome %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :estante_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
